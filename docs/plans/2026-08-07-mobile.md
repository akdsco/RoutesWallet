# mobile — responsive layout

Worktree for **TB-49**.

- TB-49: https://app.notion.com/p/Mobile-layouts-bottom-sheet-1a-list-map-toggle-1b-3b415f963d13811ea407c3dd89049264
- Base: `main` @ a808ca4

## Problem

The app is desktop-first: a fixed 392px sidebar next to the map. On a phone the
sidebar takes the whole screen, the map is a ~30px sliver, and the page scrolls
sideways — **effectively unusable**. Since the app is shared as a link
(WhatsApp → phones), this is high impact.

Make it responsive. **Pattern 1a (recommended):** map full-screen + a draggable
**bottom sheet** holding search + the route list (Google-Maps style).
**Alternative 1b:** a list/map toggle. Desktop layout stays as-is above a
breakpoint.

## Key files
- `web/src/App.tsx` — the `flex h-screen` shell (sidebar + map) → responsive
- `web/src/components/Sidebar.tsx` — becomes the sheet content on mobile
- `web/src/components/RouteMap.tsx` — map sizing / controls placement on mobile
- `web/src/index.css` — breakpoints, sheet styles

## Coordination
- **↔ `search-select` (TB-53):** the bottom sheet is the home of the route detail
  panel — share the detail component; agree which worktree lands first. This one
  is the more foundational (restructures the shell), so it's a good anchor.
- Touches RouteCard (also `a11y` TB-47) — coordinate on the card markup.

## How to work here
- `cd web && npm ci` first.
- Verify by eye at phone + desktop widths (say so honestly). Small commits.
- Full gate before push: `npm run typecheck && npm run lint && npm run format:check && npm test && npm run build`.
- Open a PR into `main` when done — the user merges.
